{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/src/server.ts" },
    { "source": "/simulations/(.*)", "destination": "/api/simulations/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
